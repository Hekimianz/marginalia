services:
  db:
    image: postgres:18
    container_name: marginalia-db
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: marginalia
    ports:
      - '5432:5432'
    volumes:
      - marginalia-pgdata:/var/lib/postgresql/data

volumes:
  marginalia-pgdata:
